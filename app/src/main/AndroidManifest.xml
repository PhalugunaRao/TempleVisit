<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.temples">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".utils.TempleApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon_lunch"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_lunch"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="@string/provider_name"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
        <activity

            android:name=".login.SplashScreenActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity"></activity>
        <activity android:name=".login.RegisterActivity"></activity>
        <activity android:name="com.temples.dashboard.DashboardActivity"></activity>
        <activity android:name=".dashboard.MainActivity"></activity>
        <activity android:name=".details.TempleDetailsPage"></activity>
        <activity android:name=".details.PackageDetailsActivity"></activity>
        <activity android:name=".profile.ProfileActivity"></activity>
        <activity android:name=".details.BookingSuccessActvity"></activity>
        <activity android:name=".details.BookingHistryDetailActvity"></activity>
        <activity android:name=".details.TermsActivity"></activity>
    </application>

</manifest>